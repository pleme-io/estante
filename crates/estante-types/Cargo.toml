[package]
name = "estante-types"
description = "Typed primitives for estante — the pleme-io shell-package manager. PkgSpec / LoadSpec / LockedPkgSpec re-exported from frost-lisp; resolver/manifest/lockfile shapes; estante's own typed config. No I/O, no network, no randomness."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
frost-lisp = { workspace = true }
tatara-lisp = { workspace = true }

serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
blake3 = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }
